<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>URL Shortener</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div>
  <h1 class="animated-gradient-text">URL Shortener</h1>
  <form action="/shorten" method="POST">
    <input type="text" name="longUrl" placeholder="Enter your long URL" required>
    <button type="submit">Shorten</button>
  </form>
  <p>
    <% if(shortUrl) { %>
      <h2>Your shortened link ✨ Ta..daaa...👇</h2>
      <p><a class="animated-gradient-text" href="<%= shortUrl %>" target="blank"><%= shortUrl %></a></p>
    <% } %>
  </p>
  </div>
</body>
</html>
